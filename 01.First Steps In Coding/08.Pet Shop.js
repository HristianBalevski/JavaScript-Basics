function petShop(input) {
    let quantityOfDogFood = Number(input[0])
    let quantityForCatFood = Number(input[1])
    let totalPrice = (quantityOfDogFood * 2.50) + (quantityForCatFood * 4)
   
   console.log(`${totalPrice} lv.`)
}
